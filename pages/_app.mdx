import { ThemeProvider } from 'next-themes'
import '../global.css'
 
This file imports global CSS and defines the App component

export default function App({ Component, pageProps }) {
  return (
    <ThemeProvider attribute="class">
      <Component {...pageProps} />
    </ThemeProvider>
  )
}
